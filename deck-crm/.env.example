# Public browser-safe settings
PUBLIC_BACKEND_API_URL=

# Server-side frontend settings (preferred)
DECK_AISTACK_BACKEND_URL="http://127.0.0.1:8080"
# Legacy compatibility for older frontend templates (used when set).
PUBLIC_DECK_AISTACK_BACKEND_URL=

# Optional frontend product slug override
PUBLIC_DECK_PRODUCT_SLUG="deck-aistack-codes"
