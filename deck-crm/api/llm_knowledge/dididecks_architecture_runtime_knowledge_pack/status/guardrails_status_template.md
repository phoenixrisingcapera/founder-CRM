# Guardrails Status

| Area | Status | Notes |
|---|---|---|
| API prefix checks | Draft | Enforce `/api/products/dididecks/*`. |
| Frontend boundary checks | Draft | Block secrets/backend logic in frontend. |
| Editor model checks | Draft | Detect local-only editor drift. |
| Surface-aware review checks | Draft | Validate scroll/play/print contexts. |
| Private route exposure checks | Draft | Ensure deck surfaces are backend access-checked. |
| Training-data privacy checks | Draft | Required before exporting feedback for model training. |
| AI change safety | Draft | AI proposes, guardrails evaluate, user accepts/rejects. |
