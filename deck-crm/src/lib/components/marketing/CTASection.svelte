<section class="panel cta-section">
  <div>
    <div class="eyebrow">Next step</div>
    <h2>Ready to turn decks into diligence-ready assets?</h2>
    <p class="muted">
      Deck AIStack is built for diligence adaptation, investor workflows, and brand-aware deck versions rather
      than generic presentation generation.
    </p>
  </div>

  <div class="actions">
    <a class="button" href="/#show-interest">Show interest</a>
    <a class="button secondary" href="/auth/sign-in">Sign in</a>
  </div>
</section>

<style>
  .cta-section {
    padding: 1.45rem;
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    align-items: end;
    background:
      radial-gradient(circle at left center, var(--glow-blue), transparent 24%),
      radial-gradient(circle at right center, var(--glow-pink), transparent 22%),
      var(--gradient-surface);
  }

  .actions {
    display: flex;
    gap: 0.8rem;
    flex-wrap: wrap;
  }

  @media (max-width: 840px) {
    .cta-section {
      flex-direction: column;
      align-items: start;
    }
  }
</style>
