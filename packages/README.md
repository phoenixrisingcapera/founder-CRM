# CRM Packages Status

These packages are mixed-status source material.

## Still Useful

- `theme/`: active token reference

## Reference / Legacy-Leaning

- `sdk/`: old CRM and DDDecks-oriented client wrappers, not the canonical contract surface
- `types/`: mixed historical type definitions, not the source of truth for the active Founder CRM backend
- `ui/`: small reference components only

For active founder CRM product work, prefer the contracts in:
- `../api/app/schemas`
- `../web/src/lib/types.ts`
