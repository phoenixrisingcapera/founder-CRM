[phases.setup]
providers = ["python"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install ."]

[start]
cmd = "sh -lc 'alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8010}'"
